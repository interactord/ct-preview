import Domain
import Platform
import SwiftUI

public struct ContentView: View {
  public init() { }

  public var body: some View {
    VStack {
      Text("Hello, world!")
    }
    .padding()
  }
}
